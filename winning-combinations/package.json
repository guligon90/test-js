{
  "name": "winning-combinations",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "rimraf ./dist && tsc",
    "update:check": "ncu",
    "update:apply": "ncu -u",
    "update:install": "rimraf yarn.lock && rimraf node_modules && yarn install"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "npm-check-updates": "^16.14.18",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  }
}
